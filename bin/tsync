#!/bin/bash

TASKS_BASE=$HOME/.gh-dotfiles

touch -d '-1 hour' $TASKS_BASE/limit
if [ limit -nt $TASKS_BASE/last_sync ]; then
  echo 'should sync'
    #send notification...
    touch $TASKS_BASE/last_sync
fi


